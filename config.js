/* Fill these two values from your Supabase project:
   Supabase dashboard -> Project Settings -> API
   - Project URL  -> SUPABASE_URL
   - anon public key -> SUPABASE_ANON_KEY
   The anon key is SAFE to publish — all security lives in the
   database's row-level-security rules (schema.sql), not in this key.

   While these placeholders are unchanged the app runs in DEMO MODE:
   fake in-browser data, no server, so you can try everything first. */
window.CLUB_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT-REF.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-KEY'
};
