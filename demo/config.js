/* Demo / training sandbox: placeholder config keeps the app in DEMO MODE —
   in-browser sample data only, no server, nothing can touch club data. */
window.CLUB_CONFIG={
  SUPABASE_URL:"YOUR-SUPABASE-URL",
  SUPABASE_ANON_KEY:"YOUR-ANON-KEY"
};
